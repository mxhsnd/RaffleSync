# 毕业晚会抽奖系统（纯静态双屏联动版） - 核心开发文档

你现在是一个资深的前端开发工程师，请严格按照以下架构、逻辑和视觉规范，为我编写一个纯静态的毕业晚会抽奖系统。

## 1. 项目概述与技术栈
* **运行模式：** 纯静态前端，无需后端服务器。必须支持跨页面通信——控制台页面（电脑主屏幕）操作，展示页面（投屏大屏幕）实时响应。
* **核心通信：** 强制使用 HTML5 `BroadcastChannel` API 实现双屏无延迟同步。
* **UI/样式：** 使用 CDN 引入 Tailwind CSS 构建现代化布局，核心特效配合原生 CSS。
* **动效库：** 使用 Anime.js 或原生 CSS (`transition`/`animation`) 处理名单滚动、卡片弹入等流畅动画。

## 2. 必须生成的文件结构
请将代码严格拆分，禁止全部写在一个 HTML 文件里：
1. `index.html` - 大屏展示页（只负责展示动画和结果）
2. `admin.html` - 后台控制页（只负责发送指令和控制流程）
3. `data.js` - 数据源（包含所有参与抽奖的毕业生名单）
4. `display.js` - 大屏页面的动效逻辑与 BroadcastChannel 接收端
5. `control.js` - 控制页的交互逻辑、去重算法与 BroadcastChannel 发送端
6. `styles.css` - 存放 Tailwind 无法直接覆盖的自定义高级特效（如毛玻璃光晕、入场关键帧）

## 3. 核心数据与严格状态管理
* **全局唯一数据池：** 三个抽奖环节必须共享同一个名单数组，一旦某人中奖，立刻将其 `isDrawn` 设为 `true`。绝不允许同一人在任何环节重复中奖。
* **名单数据结构示例（请在 `data.js` 中使用此格式）：**
  ```javascript
  const studentData = [
    { college: '软件（人工智能）学院', className: '软件24-14', name: '马希涵', studentId: '2420011416', isDrawn: false },
    { college: '安全科学与工程学院', className: '安全24-01', name: '张三', studentId: '2420011417', isDrawn: false },
    // 必须确保总计包含 5 个不同学院的数据以便测试
  ];

  4. 业务环节与 BroadcastChannel 通信协议
系统包含三个环节，控制端与展示端的数据交互必须严格遵循以下 JSON 协议格式：

环节 A & B：常规抽奖（共两轮）
在大名单中随机抽取指定数量（如 10 人或 20 人）的学生。

控制端操作： 设置抽取人数 -> 点击“开始滚动” -> 点击“停止并开奖”。

大屏动效： 屏幕中央快速跳动名字网格营造紧张感，开奖时中奖名单以 Grid 卡片形式依次弹入（带有缩放缓冲效果）。

通信协议：

{ action: 'SWITCH_ROUND', round: 'NORMAL_A' }

{ action: 'START_ROLL_NORMAL', count: 10 }

{ action: 'STOP_ROLL_NORMAL', winners: [{...}, {...}] }

环节 C：院长特定纪念品抽奖（5 个学院轮流）
每个学院抽取 1 人，大屏同时展示 5 个学院的位置，轮流开奖。支持由于学生不在场导致的“重新抽取”。

控制端操作： 页面显示 5 个学院的独立抽奖面板。点击对应学院的“抽取”；如果不在场，点击该学院的“重抽”。

重抽核心逻辑： 若点击“重抽”，必须先将前一位中奖者的 isDrawn 恢复为 false，再从该学院剩余 isDrawn === false 的人员中重新抽取一位，并设为 true。

大屏动效： 页面横向或网格排列 5 个学院的卡片（初始为空或 Logo）。收到抽取指令，对应卡片内名字飞速滚动并定格；收到重抽指令，卡片内名字碎裂或淡出，重新滚动并定格新名字。

通信协议：

{ action: 'SWITCH_ROUND', round: 'DEAN_DRAW' }

{ action: 'START_ROLL_DEAN', college: '软件（人工智能）学院' }

{ action: 'STOP_ROLL_DEAN', winner: {...} }

{ action: 'REROLL_DEAN', college: '软件（人工智能）学院', previousWinnerId: '2420011416' }

5. UI 审美与视觉规范 (极其重要)
请彻底摒弃传统的纯色背景、死板边框和默认字体。你需要打造一个具有现代科技感、高级感的晚会大屏。

Glassmorphism (拟态玻璃) 风格： 所有内容卡片必须使用半透明毛玻璃效果。Tailwind 样式参考：bg-white/10 backdrop-blur-md border border-white/20 shadow-xl rounded-xl。

排版与字体： 使用无衬线字体（sans-serif），字号要足够大，粗细搭配合理。名字等核心信息需使用白色（text-white）并配合微弱的发光文字阴影（text-shadow），确保后排观众清晰可见。

色彩点缀： 在卡片边缘或中奖状态时，加入科技蓝、霓虹紫或琥珀金的微弱光晕（Glow 效果）。

背景预留： 大屏 index.html 的 body 需要设置为全屏背景图（我后续会用 AI 生成一张暗色调星空科技风格的 bg.jpg 替换进去），所以整体 UI 必须是暗色模式下的亮色发光文字风格。

绝不生硬： 任何元素的出现、消失、状态切换，都必须带有淡入淡出（opacity transition）或轻微的位移（translate-y）。

6. 执行要求
请阅读并理解以上所有要求后，直接开始分步骤输出代码。优先搭建基础 HTML 骨架和 data.js，然后实现 BroadcastChannel 的底层通信，最后再逐步填充复杂的抽奖逻辑和高级样式。